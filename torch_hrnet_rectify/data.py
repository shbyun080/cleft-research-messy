from config import config as cfg


def get_cleft_data():
    # Load label csv
    # Loop through each line
        # Get image
        # Resize to (256, 256)
        # Resize labels
        # Make labels (256, 256)

    cfg.DATA.IMAGE_DIR
    cfg.DATA.LABEL_FILE


def get_cleft_target():
    # Return rectification target for cleft
    pass


def transform_labels(label, center, scale):
    # Relocate the label according to cropping
    pass
